const Header = () => {
    const header = {
        padding: "10px",
        color: "#43CBC3",
        fontWeight: "bold",
        fontSize: "20px",
        textAlign: "center",
        zIndex: "10",
    };

    return (
        <div style={header}>career passport</div>
    );
};

export default Header;