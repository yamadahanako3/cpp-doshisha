const Header = () => {
    const header = {
        color: "#43CBC3",
        fontWeight: "bold",
        fontSize: "20px",
        textAlign: "center"
    };

    return (
        <div style={header}>career passport</div>
    );
};

export default Header;